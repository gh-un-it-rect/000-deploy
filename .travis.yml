dist: trusty
sudo: required
notifications:
  email: false
  
language: bash

before_script:
  - pwd
  - ls -ltra
  - git version
  
script:
- chmod +x scripts/deploy.sh
- scripts/deploy.sh '101-tj-vicboma1'

after_script:
  - pwd
  - ls -ltra
